<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compact, dark action button used across the control panels. -->
    <style name="MecButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_bg</item>
        <item name="android:textColor">@color/mec_text</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">13sp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">42dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:layout_marginEnd">6dp</item>
    </style>

    <!-- Segmented (selectable) variant for the pose-model picker. -->
    <style name="MecSegButton" parent="MecButton">
        <item name="android:background">@drawable/seg_model</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Collapsible section header bar. -->
    <style name="MecHeader">
        <item name="android:background">@drawable/bg_header</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@color/mec_accent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>

    <!-- Numeric input field in the co-localization panel. -->
    <style name="MecField" parent="Widget.AppCompat.EditText">
        <item name="android:inputType">numberDecimal|numberSigned</item>
        <item name="android:textColor">@color/mec_text</item>
        <item name="android:textColorHint">@color/mec_hint</item>
        <item name="android:textSize">13sp</item>
        <item name="android:layout_marginEnd">4dp</item>
    </style>
</resources>
